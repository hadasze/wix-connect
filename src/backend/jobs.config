{
    "jobs": [{
            "functionLocation": "/data-methods.js",
            "functionName": "clearOldSentUsers",
            "description": "Run every hour to clear old sent users",
            "executionConfig": {
                "cronExpression": "0 * * * *"

            }
        }

    ]
}